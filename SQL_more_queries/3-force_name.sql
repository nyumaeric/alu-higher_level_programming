-- Script that creates the table force_name in database provided.
-- Query that creates the table force_name in database provided.
CREATE TABLE IF NOT EXISTS force_name(
    id INT,
    name VARCHAR(256) NOT NULL
);
